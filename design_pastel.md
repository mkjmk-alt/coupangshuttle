# Coupang Shuttle Map — Pastel Map 디자인 시스템

## 1. 기준과 적용 범위

사용자가 제작한 **지도·노선·원형 버스 정류장 표지판 아이콘**을 웹사이트의 시각 기준으로 삼는다. 원본의 파란 표지판, 민트색 공원, 아이보리 도로, 코랄색 정류장을 모두 보존한다. 버스만 추출하거나 아이콘 전체를 코랄색으로 바꾸지 않는다.

이 문서는 현재 웹페이지에 적용하는 파스텔 테마의 기준이다. 기존 `design.md`와 `design_new.md`는 수정하지 않고 독립된 디자인 문서로 보존한다. 문서 보존은 이전 소스 전체의 Git 백업을 의미하지 않는다.

- 이미지 자산: `public/shuttle-pastel.jpg` — 사용자 제공 JPEG 원본을 변형 없이 복사.
- 스타일 진입점: `app/globals.css` → `app/pastel.css`.
- 공개 화면: 홈, 노선 선택, 지도, 시간표, 비교함, 광고 안내, 개인정보 동의, 정책·문의.
- 관리자 화면: 기존 기능과 배치를 유지하며 공통 색상·폰트·입력 스타일을 공유.
- 데이터, API, 문의 주소, 광고 조건, 관리자 인증·저장 로직은 이번 테마 변경 범위에서 변경하지 않는다.

## 2. 디자인 방향

**밝은 동네 지도 위에서 오늘의 출퇴근 경로를 찾는 느낌.** 파스텔은 넓은 배경과 장식에, 더 짙은 색은 글자와 행동에 사용한다.

1. 블루: 기본 브랜드 색, 선택, 버튼, 링크, 포커스.
2. 민트: 최신 데이터·정상 상태, 안내 카드, 부드러운 배경.
3. 아이보리: 페이지·카드의 넓은 표면.
4. 코랄: 노선의 정류장처럼 작은 강조 요소. 일반 버튼 전체를 코랄로 칠하지 않는다.
5. 옐로: 공식 공지 우선·주의 안내. 블루/민트 장식과 역할을 구분한다.

아이콘의 은은한 입체감은 카드의 얕은 그림자와 버튼의 짧은 그라디언트로 이어간다. 실제 지도 위에는 장식용 도로나 패턴을 겹치지 않는다.

## 3. 색상 토큰

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--paper` | `#F7F6F2` | 페이지 배경·아이콘의 도로색 |
| `--card` | `#FFFEFB` | 카드·입력·표면 |
| `--ink` | `#293B4E` | 제목·주요 본문 |
| `--muted` | `#626F79` | 설명·보조 글자 |
| `--line` | `#DFE5E5` | 카드 테두리·표 구분 |
| `--soft-line` | `#EBEFEB` | 약한 구분선 |
| `--blue` | `#7D9CE4` | 아이콘과 이어지는 장식 블루 |
| `--primary` | `#486DB5` | 흰 글자를 쓰는 버튼·주요 링크 |
| `--primary-dark` | `#36558F` | hover·선택 글자 |
| `--blue-soft` | `#EDF2FF` | 선택된 메뉴·안내 배경 |
| `--mint` | `#65AD94` | 상태 점·장식 |
| `--mint-dark` | `#326D58` | 민트 배경 위 글자 |
| `--mint-soft` | `#EAF6F1` | 최신 상태·운영 안내 |
| `--park` | `#CBE6CF` | 홈 장식의 공원색 |
| `--water` | `#C7E5ED` | 배경의 옅은 하늘색 |
| `--coral` | `#E96F50` | 정류장 포인트·소량 강조 |
| `--coral-dark` | `#AB482F` | 코랄 관련 작은 글자 |
| `--coral-soft` | `#FFF0E9` | 강조색의 연한 표면 |
| `--violet` | `#8C7AB9` | 노선 비교 보조색 |
| `--violet-soft` | `#F2EDFB` | 비교 보조 표면 |
| `--yellow` | `#C49C40` | 주의 아이콘·선 |
| `--yellow-soft` | `#FFF7DF` | 공식 공지 주의 배경 |

기존 컴포넌트의 `indigo-*` 유틸리티는 블루 계열에 연결한다. `emerald-*`는 민트, `purple-*`는 바이올렛, `slate-*`는 파란 기가 약한 본문·중성색에 연결한다. 클래스 이름을 바꾸기 위해 기능 코드를 일괄 수정하지 않는다.

`--blue`는 장식색이다. 작은 흰 글자를 `#7D9CE4` 위에 바로 놓지 않고, 주요 버튼에는 더 짙은 `--primary`를 사용한다. 오류·삭제의 빨간색과 노선별 식별 색상은 그대로 구분한다.

## 4. 글꼴과 정보 계층

- 본문·제목·브랜드: `Pretendard`, `Apple SD Gothic Neo`, `Noto Sans KR`, 시스템 sans-serif. 브랜드는 작은 화면에서도 읽히도록 본문과 같은 계열의 굵은 글자를 사용한다.
- 센터·노선 코드: 시스템 monospace.
- 외부 웹폰트 다운로드에 의존하지 않는다. 설치되지 않은 글꼴은 시스템 폴백을 사용한다.
- 홈 제목은 데스크톱 40~48px, 모바일 30~34px, 굵기 750, 행간 1.2. 기울임을 사용하지 않는다.
- 섹션 제목은 20~28px, 본문은 13~16px, 보조 라벨은 10~12px.
- 한국어 제목과 짧은 안내는 `word-break: keep-all`로 단어 중간 끊김을 줄인다.

## 5. 아이콘·브랜드 사용

- 데스크톱 사이드바·푸터: 아이콘 48×48px, 둥근 모서리 15px.
- 모바일 상단: 아이콘 42×42px, 둥근 모서리 13px.
- 홈 상단: 데스크톱 160px. 모바일은 상단 로고 아이콘과 중복되지 않도록 소개 그림을 숨긴다. 버스·노선·지도 구도 전체를 표시하는 원칙은 유지한다.
- 이미지를 `object-fit: contain`으로 표시하며 임의 확대·크롭·색 필터를 적용하지 않는다.
- 모서리는 CSS로 둥글게 처리한다. JPEG 원본 자체를 편집하지 않는다.
- 홈의 큰 이미지는 의미를 설명하는 대체 텍스트를 제공한다. 링크 안의 작은 아이콘은 링크의 접근 가능한 이름이 있으므로 빈 대체 텍스트를 사용한다.
- 작은 파비콘은 향후 실제 축소 검수가 필요한 별도 산출물이다. 이번 작업은 제공한 원본의 브랜드·홈 적용을 우선한다.

## 6. 레이아웃

### 앱 셸

- 980px 초과: 248px 고정 사이드바, 오른쪽 본문.
- 980px 이하: 72px 모바일 상단 바와 하단 내비게이션. 안전 영역을 반영한다.
- 메뉴: 노선 조회, 운영·데이터 정책, 개인정보, 약관, 문의.
- 현재 경로만 블루 선택 상태로 표시하고 `aria-current="page"`를 사용한다.
- 푸터는 동일한 아이콘·브랜드명과 정책·문의 링크를 사용한다.

### 홈

1. 업데이트 시각을 민트 배지로 표시.
2. 왼쪽에 제목·설명·업데이트 시각, 오른쪽에 축소한 전체 아이콘을 배치한 소개 영역.
3. 소개 영역 전체에는 아이콘의 노선을 확장한 옅은 곡선과 코랄 정류장 점을 배경 장식으로 깔되, 제목·설명·아이콘보다 뒤에 둔다. 아이콘 안의 지도 노선과 중복되어 보이지 않도록 단일 배경 노선만 사용한다.
4. 별도 장식 통계 카드 없이 센터·근무조·노선 선택을 첫 번째 기능 카드로 배치.
5. 탑승 전 주의 안내는 조회 카드 안에 두고, 지도·시간표·비교·제휴 안내·도움말의 기존 흐름을 유지.
6. 620px 이하에서는 소개 그림을 숨기고 제목을 왼쪽 정렬하며, 조회 카드가 고정 하단 메뉴 위에 먼저 보이도록 한다.

센터 목록·노선·정류장 수를 장식용 가상 통계로 표시하지 않는다. 실제 업데이트 시각은 기존 메타데이터를 사용한다.

## 7. 공통 컴포넌트

- 카드: 아이보리 표면, 24px 모서리, 1px 테두리, 옅은 푸른 그림자. 내부 내용의 여백은 기존 유틸리티를 활용.
- 입력: 최소 48px 높이, 14px 모서리, 밝은 중성 배경, 16px 선택 값, 블루 포커스. 선택 화살표 자리 확보.
- 버튼: `--primary` 중심의 약한 블루 그라디언트, 13px 모서리. hover 이동은 1px 이내.
- 민트 상태 배지: 진한 민트 글자·상태 점. 색만으로 의미를 전달하지 않는다.
- 지도: 24px 카드 프레임, 원래 지도 타일·핀·노선색 유지. 지도 빈 상태·실패 메시지 유지.
- 시간표: 밝은 헤더, 중성색 구분선, 시간·선택된 정보에 진한 블루. 모바일 카드형 정류장 유지.
- 비교함: 블루·바이올렛 보조 표면. 최대 5개와 기존 잠금 해제 조건 유지.
- 광고·공식 공지: 옐로 표면을 사용하고 외부 서비스 브랜드색은 그대로 둔다.
- 도움말: 민트·블루의 옅은 카드, 클릭 가능한 summary와 열림 내용 구분.

## 8. 공개 정책·문의·관리자

- 정책 문서는 같은 배경·폰트·링크를 사용하고 본문의 의미·날짜·문의 주소는 변경하지 않는다.
- 운영 원칙은 민트, 개인정보 안내는 블루, 약관의 주의 고지는 옐로.
- 문의 화면은 카카오톡·이메일 카드와 제보 안내를 유지한다. 카카오톡 버튼은 고유 노란색 유지.
- 관리자 인증 화면·입력·상태 카드에 공통 토큰을 적용한다. 저장·편집·정류장 데이터·API 계약을 변경하지 않는다.
- 광고 동의 및 비교 기능 모달은 기존 동작을 유지하고, 기존 z-index 위계를 깨지 않도록 사이드바는 100을 넘기지 않는다.

## 9. 반응형·접근성

- 검수 기준 폭: 데스크톱 1440px, 중간 화면 820px, 모바일 390px와 320px.
- 화면 바깥으로 가로 스크롤이 생기지 않게 한다. 긴 정책 표는 해당 표 영역에서만 가로 스크롤한다.
- 키보드 포커스는 3px 진한 블루 outline과 3px offset.
- 본문·주요 버튼 글자의 색 대비를 확인한다. 파스텔 배경을 쓰더라도 글자까지 흐리게 만들지 않는다.
- `prefers-reduced-motion: reduce`에서는 장식과 hover의 이동을 최소화한다.
- 이미지의 크기를 명시해 로딩 중 레이아웃 이동을 줄인다.

## 10. 구현과 검수

| 파일 | 역할 |
| --- | --- |
| `design_pastel.md` | 이번 파스텔 테마와 조회 중심 개선 기준 |
| `public/shuttle-pastel.jpg` | 사용자 제공 아이콘 원본 |
| `app/globals.css` | Tailwind·폰트·파스텔 스타일 진입점 |
| `app/pastel.css` | 토큰, 셸, 홈, 카드, 정책, 반응형 |
| `app/layout.tsx` | 공통 브랜드 이미지 연결 |
| `app/page.tsx` | 아이콘을 사용한 홈 소개 영역 |
| `components/SiteNavigation.tsx` | 현재 경로에 맞는 메뉴 표시 |

검수는 문서·원본 보존 확인 → 프로덕션 빌드·변경 파일 린트 → 실제 브라우저에서 홈·센터 선택·노선 시간표·정책·문의·관리자 접근 상태 → 모바일 폭·콘솔 오류 확인 순서로 진행한다. 기존 저장소 전체 린트 오류는 이번 변경에서 발생한 오류와 구분하여 보고한다.

배포는 별도 작업이다. 로컬 반영과 공개 사이트 배포를 구분하여 기록한다.
