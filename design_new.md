# Coupang Shuttle Map 디자인 시스템

> 이 문서는 기존 `design.md`의 시각 테마를 현재 쿠팡 셔틀 서비스의 기능에 맞게 다시 정의한 구현 기준입니다. 휴가 기록 서비스의 콘텐츠와 화면 구조는 사용하지 않고, 현재 Next.js 앱의 노선 조회·지도·시간표·노선 비교·관리자 편집·정책·문의 기능을 유지합니다.

## 1. 제품 인상과 목표

`Coupang Shuttle Map`은 쿠팡 물류센터 근무자가 출퇴근 셔틀 노선과 정류장 정보를 빠르게 확인하는 비공식 참고 서비스입니다.

- 센터·근무조·노선 선택을 첫 화면에서 가장 먼저 발견하게 한다.
- 지도와 정류장 시간표를 같은 흐름에서 확인하게 한다.
- 차분한 종이색 표면과 세리프 제목으로 휴식에 가까운 인상을 만들되, 출퇴근 정보의 정확성과 긴급성을 해치지 않는다.
- 코랄은 주요 행동과 현재 선택, 민트는 정상·확인 상태, 블루는 안내·보조 선택, 옐로는 주의·외부 정보 상태에 사용한다.
- 데스크톱·태블릿·모바일에서 센터 선택, 지도, 시간표, 비교 기능을 유지한다.
- 모든 화면에서 실제 운행 정보는 소속 센터의 최신 공식 공지가 우선임을 분명히 한다.

## 2. 기능 범위와 현재 동작 보존

디자인 변경은 표현과 배치를 바꾸며 데이터 형식, API 계약, 기능 동작은 바꾸지 않습니다.

### 공개 이용자 기능

1. 홈(`/`)에서 셔틀 데이터를 불러온다.
2. 물류센터를 선택하면 해당 센터의 근무조와 노선을 표시한다.
3. 근무조를 선택하면 노선 목록을 좁힌다. 근무조를 선택하지 않으면 전체 근무조 노선을 조회할 수 있다.
4. 노선을 선택하면 카카오 지도에 정류장 핀을 표시하고 정류장 순서·시각·주소·안내를 시간표에 표시한다.
5. 센터별 공식 셔틀 안내 링크를 새 창으로 연다.
6. 선택한 노선을 비교함에 최대 5개까지 담는다.
7. 비교 시작 전 보상형 광고 또는 웹 제휴 링크 미션을 거치고, 완료 후 1시간 동안 비교 기능을 사용할 수 있다.
8. 비교 모드에서는 선택한 노선의 정류장을 시간순 통합 시간표로 표시하고 지도에서 노선별 색상을 구분한다.
9. 광고 쿠키 동의·거부, 쿠키 설정, 개인정보·약관·운영 정책·문의 페이지를 기존 동작대로 제공한다.

### 관리자 기능

`/editor`의 편집 키 인증, 전체 데이터 로딩, 센터·근무조·노선 검색, 정류장 편집, 주소·좌표·시각 수정, 지도 확인, 오류 후보(속도·시간·거리) 검토, 스킵 저장·복원, 기본 데이터로 노선 복원, 변경 로그 확인, 저장·병합·GitHub 배포 동작을 유지한다.

관리자 화면의 색상과 카드 스타일은 공개 화면과 같은 토큰을 사용하되, 오류 검토 영역은 충분한 대비를 확보한다.

## 3. 시각 토큰

아래 값은 기존 `design.md`의 테마 값을 그대로 사용합니다.

### 색상

| 토큰 | 값 | 쿠팡 셔틀 화면 용도 |
| --- | --- | --- |
| `--paper` | `#f7f6f2` | 전체 페이지 배경 |
| `--card` | `#fffefb` | 검색·비교·지도·시간표 카드 표면 |
| `--ink` | `#242422` | 제목·본문 |
| `--muted` | `#77766f` | 보조 설명·비활성 텍스트 |
| `--line` | `#e7e4dc` | 카드·입력 필드 테두리 |
| `--soft-line` | `#efede7` | 표 구분선·약한 경계 |
| `--coral` | `#e96f50` | 주요 CTA, 현재 선택, 비교 시작, 선택된 노선 |
| `--coral-dark` | `#cc593d` | 코랄 버튼 hover/pressed |
| `--coral-soft` | `#fff0e9` | 선택된 센터·노선, 주요 상태 배경 |
| `--mint` | `#65ad94` | 정상 로딩·최신 반영·확인 완료·지도 표시 상태 |
| `--mint-soft` | `#eaf6f1` | 정상 상태 배경 |
| `--blue` | `#7d9ce4` | 도움말·공식 링크·보조 선택 상태 |
| `--blue-soft` | `#edf2ff` | 안내 패널·빈 상태 |
| `--violet` | `#9a82cb` | 비교 모드·보조 노선 구분 |
| `--violet-soft` | `#f2edfb` | 비교 카드 배경 |
| `--yellow` | `#d3a73e` | 공식 공지 확인 주의·광고/외부 링크 안내 |
| `--yellow-soft` | `#fff7d9` | 비공식 서비스 고지·주의 배경 |

노선별 지도 핀 색상은 `utils/color.ts`의 기능을 유지합니다. 다만 기본 UI 상태색과 충돌하지 않도록 코랄·민트·블루·바이올렛 계열의 충분한 명도 차이를 사용하고, 색상만으로 노선을 구분하지 않고 노선명·센터명·시간을 함께 표시합니다.

페이지 배경은 `--paper` 위에 우측 상단의 옅은 복숭아색 radial gradient를 겹칩니다. 넓은 영역에 강한 그라디언트를 사용하지 않고 CTA와 선택 상태에만 색을 집중합니다.

### 글꼴

| 역할 | 글꼴 | 기본 크기·굵기 | 적용 예 |
| --- | --- | --- | --- |
| 본문·버튼·라벨 | `Pretendard`, `Apple SD Gothic Neo`, `Noto Sans KR`, 시스템 sans-serif | 10–16px, 400–750 | 설명, 폼, 내비게이션, 시간표 |
| 로고·브랜드명 | `Yeoncha Brand Serif`, `Noto Serif KR`, serif | 17–18px, 600 | `Coupang Shuttle Map` 브랜드명 |
| 큰 제목·날짜·시간 | `Georgia`, `Noto Serif KR`, serif | 제목 24–58px, 시간 24px까지 | 홈 제목, 업데이트 날짜, 정류장 시각 |
| 센터 코드·노선 코드 | `ui-monospace`, `SFMono-Regular`, `Menlo`, monospace | 컨텍스트에 맞춤 | `ANS4`, 노선 코드, 데이터 버전 |
| 섹션 eyebrow | 본문 글꼴 | 11–12px, 800, 자간 약 `.06em` | `ROUTE FINDER`, `STOP TIMES`, `DATA STATUS` |

현재 저장소에 `design.md`가 지정한 `app/assets/fonts/noto-serif-kr-semibold-brand.woff2` 파일은 없습니다. 구현 시에는 `Noto Serif KR`·`Georgia` 폴백을 우선 사용하고, 브랜드용 번들 폰트를 추가할 때만 `@font-face`를 연결합니다. 폰트가 없어도 레이아웃이 바뀌지 않도록 글꼴 로딩에 의존하는 폭 계산은 사용하지 않습니다.

### 형태·그림자·모션

- 기본 카드: 반지름 `22px`, `1px solid var(--line)`, 낮은 대비의 그림자.
- 작은 카드·입력·버튼: 반지름 `11–16px`.
- pill/badge: 반지름 `99px`.
- 기본 그림자: `0 18px 50px rgba(43, 40, 32, .055)`.
- 버튼 hover는 색상을 한 단계 어둡게 하고 `translateY(-1px)`만 적용한다.
- 화면 진입은 opacity와 `translateY(6px)`를 약 420ms 동안 사용한다.
- `prefers-reduced-motion: reduce`에서는 전환·펄스·스피너 외 장식 애니메이션을 최소화한다.
- 지도 컨테이너는 카드와 같은 테두리·반지름을 사용하되, 지도 조작 영역을 가리지 않도록 과한 그림자를 사용하지 않는다.

## 4. 앱 셸과 반응형 레이아웃

### 데스크톱 앱 셸

- 전체 셸은 `.app-shell`이며 최소 높이는 `100vh`다.
- 사이드바는 폭 `248px`, 뷰포트에 고정하고 반투명 `--card`·blur·우측 경계를 사용한다.
- 본문 `.page-shell`은 사이드바 폭만큼 왼쪽 여백을 둔다.
- 상단 브랜드 영역은 약 `88px` 높이, 좌우 패딩은 `clamp(26px, 5vw, 72px)`다.
- 메인 콘텐츠는 최대 폭 `1280px`, 상단 `56px`, 하단 `100px` 패딩으로 중앙 정렬한다.
- 공개 내비게이션은 `노선 조회`, `운영·데이터 정책`, `개인정보처리방침`, `이용약관`, `문의·제보`를 제공한다.
- `/editor`는 일반 공개 내비게이션에 노출하지 않고 직접 접근 시 제한된 관리자 화면으로 표시한다.

### 모바일 셸

- `820px` 이하에서는 사이드바를 하단 고정 내비게이션으로 바꾼다.
- 하단 내비게이션은 `env(safe-area-inset-bottom)`을 반영하고, 모달이 열리면 숨긴다.
- 본문은 `100dvh`와 키보드 영역을 고려한다.
- 지도·시간표·비교함의 기능은 유지하고, 열 수와 내부 여백만 줄인다.

### 여백과 반응형 기준

컴포넌트 간격은 `8, 10, 12, 14, 16, 18, 20, 24, 28, 32, 36, 44, 56, 72px`를 사용합니다. 작은 요소 안에서는 8–14px, 카드 내부에서는 20–32px, 주요 섹션 사이에서는 24–56px를 우선합니다.

| 기준 폭 | 동작 |
| --- | --- |
| `1120px` 이하 | 검색·비교·안내 그리드 축소 |
| `980px` 이하 | 시간표를 카드형 행으로 전환하고 지도·결과 영역을 세로화 |
| `820px` 이하 | 사이드바를 모바일 내비게이션으로 전환, 비교·광고 모달을 세로화 |
| `620px` 이하 | 센터·근무조·노선 선택 컨트롤을 줄바꿈하고 요약 pill 축소 |
| `560px` 이하 | 메인·상단 패딩을 14px 수준으로 축소, 제목·카드·지도 높이 축소 |
| `420px` 이하 | 검색 컨트롤을 1열, 비교 액션을 주요 버튼 전체 폭으로 배치 |
| `390px` 이하 | 월·날짜 대신 사용되는 업데이트 라벨과 정류장 메타를 추가 축소 |

## 5. 공통 컴포넌트

### 버튼과 링크

- Primary: `--coral` 배경, 흰색 텍스트, 최소 높이 `48px`, 좌우 패딩 `20px`, 반지름 `13px`, 굵기 `750`.
- Compact primary: 최소 높이 `40px`, 반지름 `11px`, 카드 헤더의 공식 링크·비교 액션에 사용한다.
- Secondary: `--card` 배경, `--line` 테두리, 높이 약 `41px`, 반지름 `11px`, muted 텍스트.
- Text link: 배경과 테두리 없이 사용하며, 외부 링크 아이콘 또는 화살표만 코랄·블루로 강조한다.
- 모든 버튼과 입력은 `font: inherit`로 본문 글꼴을 공유한다.
- 아이콘만 있는 닫기·삭제·지도 액션에는 `aria-label`을 제공한다.

### 카드와 상태 배지

카드는 `var(--card)` 표면, `var(--line)` 테두리, 낮은 대비 그림자를 사용합니다. 카드 안에서는 제목 → 설명 → 수치 또는 행동의 순서를 지킵니다.

- 데이터 최신 상태: 민트 pill (`최신 데이터`, `센터 파일 로딩 완료`).
- 공식 링크·도움말: 블루 pill 또는 링크.
- 비교 모드: 바이올렛 soft 표면과 노선별 컬러 칩.
- 비공식 서비스·운행 변경 주의: 옐로 soft 배경과 텍스트 고지.
- 오류·저장 실패: 오류 원인을 해당 컨트롤 가까이에 두고 코랄 계열보다 충분히 구분되는 오류 텍스트를 사용한다.

### 입력·선택 컨트롤

- select·검색 입력은 높이 `44–48px`, 반지름 `11px`, `#faf9f6` 또는 `#fbfaf7` 표면을 사용한다.
- 포커스 시 `--coral` 계열 `3px` outline과 `2px` offset을 제공한다.
- 비활성 센터·근무조·노선 선택은 흐린 텍스트만으로 표현하지 않고 표면·테두리·커서 상태를 함께 바꾼다.
- 로딩·실패 메시지는 관련 선택 그룹 바로 아래에 배치한다.
- 현재 선택된 센터·근무조·노선은 select 외에 요약 라벨로도 노출해 지도와 시간표의 범위를 알 수 있게 한다.

### 지도

- 카카오 지도는 `kakao-map-container` 카드 안에 배치한다.
- 선택 전에는 지도 대신 `먼저 물류센터를 선택해 주세요` 빈 상태를 표시한다.
- 지도 오버레이에는 현재 센터·근무조 또는 `노선 비교 모드 (n개 노선)`를 표시한다.
- 핀에는 정류장명과 순서를 제공하되, 많은 핀이 겹칠 때 지도를 가리지 않도록 상세 정보는 시간표와 연동한다.
- 지도 API 키 없음·로드 실패 상태도 지도 영역의 최소 높이를 유지한 채 안내 카드로 표시한다.

### 노선 비교함과 광고 모달

- 비교함은 `--violet-soft` 배경 카드로 표시한다.
- 각 항목에는 센터명, 근무조, 노선명, 노선 컬러 칩, 제거 버튼을 고정된 순서로 배치한다.
- 최대 5개 제한과 현재 잠금 해제 잔여 시간을 배지로 표시한다.
- 잠금 상태에서는 광고 시청 또는 웹 제휴 링크 미션의 목적과 1시간 이용 조건을 모달 안에서 설명한다.
- 모달 배경은 어두운 반투명 overlay와 blur를 사용하고, 폭은 `min(520px, calc(100vw - 28px))`, 반지름은 `22px`로 한다.
- 모달이 열린 동안 배경 스크롤과 모바일 하단 내비게이션을 제어한다.
- 잠금 해제 완료는 `aria-live` 상태 문구와 시각적 확인 문구를 함께 제공한다.

### 시간표

- 섹션 eyebrow는 `STOP TIMES`, 제목은 `정류장·운행 시각` 또는 `비교 노선 통합 시간표`를 사용한다.
- 데스크톱에서는 순서·시각·정류장명·노선/근무조·위치/탑승 안내를 표로 정렬한다.
- `980px` 이하에서는 각 정류장을 독립 카드형 행으로 바꾸고 순서·시각·노선·주소·안내의 영역을 일정하게 유지한다.
- 시각은 세리프 또는 모노스페이스로 강조하고, 정류장명과 주소는 산세리프로 읽기 쉽게 둔다.
- 비교 모드에서는 센터명과 노선명을 색상 칩뿐 아니라 텍스트로 함께 표시한다.
- 결과가 없으면 최소 높이를 유지한 빈 상태를 표시한다.

### 광고·개인정보 동의

- `CoupangBanner`는 지도와 시간표 사이 또는 결과 이후에 배치하고, 핵심 노선 정보와 시각적으로 경쟁하지 않게 한다.
- 제휴 링크 안내는 `--yellow-soft` 계열의 낮은 대비 카드로 표현한다.
- `PrivacyConsent`는 서비스 사용에 필요한 기능과 선택적 광고 쿠키를 분리해 설명한다.
- 광고 쿠키 거부 시 노선 조회·지도 기능을 계속 이용할 수 있다는 점을 명확히 한다.

## 6. 화면별 구성

### 홈 `/`

1. `ROUTE FINDER · YYYY.MM.DD` eyebrow와 `내 출퇴근 노선을 한눈에 봐요` 제목, 짧은 서비스 설명을 둔다.
2. 최신 데이터와 공식 공지 우선 원칙을 `DATA STATUS` 카드로 표시한다.
3. 비공식 서비스·운행 변경 가능성을 옐로 주의 카드로 고지한다.
4. `센터별 셔틀 노선 찾기` 검색 카드에 물류센터 → 근무조 → 노선 순서의 선택 컨트롤과 센터 공식 셔틀 안내 링크를 둔다.
5. 선택된 센터·근무조·노선의 요약 정보를 작은 stat strip으로 표시한다.
6. 선택한 노선을 비교함에 추가하고, 비교함 카드에서 제거·비교 시작·비우기를 제공한다.
7. 선택 전에는 빈 상태, 선택 후에는 카카오 지도 카드를 표시한다.
8. 지도 아래에 `STOP TIMES` 시간표를 표시한다.
9. `CoupangBanner`와 이용 안내·도움말 details를 배치한다.

홈의 정보 우선순위는 `센터 선택 → 근무조·노선 선택 → 지도 → 정류장 시간표 → 비교·도움말`입니다. 현재 셔틀 조회 기능을 휴가 잔여일수 카드나 휴가 달력으로 바꾸지 않습니다.

### 운영 및 데이터 정책 `/operations`

`OPERATIONS & DATA` eyebrow, 정책 제목, 기준일을 세리프 제목과 종이색 카드로 구성합니다. 자동 배포, 관리자 수동 저장, 로그 보관, 업데이트 시각, 오류 제보 절차, 문의 채널의 내용을 유지합니다. 정책 본문은 행간 약 `1.9`, 섹션 사이 24–56px를 사용합니다.

### 개인정보처리방침 `/privacy`

지도 API, 광고 쿠키, 문의·제보, 로컬 저장값, 관리자 스킵 상태, Cloudflare·Kakao·Google 처리 범위를 기존 내용대로 유지합니다. 표와 경고 카드는 `--card`, `--line`, `--yellow-soft` 토큰을 사용합니다.

### 이용약관 `/terms`

비공식성, 공식 공지 우선, 데이터 업데이트, 외부 서비스, 관리자 편집 키, 책임 범위를 기존 내용대로 유지합니다. 첫 번째 공식 공지 경고 카드는 `--yellow-soft`로 강조합니다.

### 문의·제보 `/contact`

`CONTACT & REPORT` eyebrow와 문의 안내를 두고 카카오톡 채널, 이메일 문의, 제보 시 필요한 센터·근무조·노선·정류장·변경 전후 내용, 개인정보 주의 카드를 제공한다. 연락 방법은 현재 링크와 주소를 그대로 유지합니다.

### 관리자 편집 `/editor`

- 제한 접근 화면은 공개 화면보다 대비를 높인 카드형 인증 영역으로 구성한다.
- 상단에 최신 자동 배포일·최신 수동 변경일을 stat 카드로 표시한다.
- `HEALTH MONITOR`는 오류 유형별 색상과 텍스트를 함께 사용한다.
- 스킵 관리, 센터·근무조·노선 선택, 정류장 편집, 지도·사진 확인, 변경 로그, 배포 가이드는 기존 순서를 유지한다.
- 저장 중·저장 성공·저장 실패·배포 대기 상태를 `aria-live`와 상태 배지로 알린다.
- 편집 키와 일반 이용자 개인정보는 화면이나 변경 로그에 노출하지 않는다.

## 7. 데이터 로딩과 상태 표현

현재 데이터 로딩 동작을 유지합니다.

- `/data/shuttle_index.json`이 있으면 센터 목록과 버전을 먼저 불러온다.
- 센터 선택 때 `/data/centers/{센터코드}.json?v={버전}`을 지연 로딩한다.
- 인덱스가 없으면 `/data/shuttle_data.json`으로 폴백한다.
- 로딩 상태는 지도·시간표 자리를 대체하지 않고 최소 영역을 유지한다.
- 센터 로딩 실패, 지도 API 키 없음, 지도 로딩 실패, 노선 없음, 비교 노선 없음 상태에 각각 짧은 원인과 다음 행동을 표시한다.
- 업데이트 시각은 `shuttle_meta.json`의 `lastUpdated`를 기존과 같은 기준으로 표시한다.
- 디자인 변경으로 JSON 키, 센터 코드, 근무조명, 노선명, 정류장 순서·좌표·시각을 변경하지 않는다.

## 8. 상태와 상호작용 규칙

- hover/pressed/selected/disabled 상태는 색상뿐 아니라 테두리·outline·명도 변화로도 구분한다.
- 키보드 포커스는 모든 버튼·링크·select·입력에 보이는 코랄 outline을 제공한다.
- 비교함 제거·비우기와 광고 모달 닫기는 현재 동작을 유지하고, 되돌리기 어려운 데이터 삭제는 확인 절차를 거친다.
- 저장·배포·스킵 복원 결과는 `aria-live` 영역과 시각적 toast 또는 상태 문구로 알린다.
- 새 센터 선택 시 근무조·노선 선택 상태를 초기화하고, 해당 목록의 첫 항목 선택 규칙을 유지한다.
- 비교 기능의 잠금 만료는 로컬 저장공간의 `compare_unlocked_until` 값을 사용하며, 만료 시 비교 모드를 종료한다.
- 외부 공식 셔틀 링크, 카카오 지도, 카카오톡, 쿠팡 제휴 링크는 새 창·외부 이동임을 링크 텍스트나 아이콘으로 알린다.

## 9. 접근성·모바일 안전 영역

- 제목 계층과 `nav`, `main`, `section`, `footer` landmark를 화면 구조와 일치시킨다.
- 아이콘만 있는 버튼에는 `aria-label`을 제공한다.
- 선택된 센터·근무조·노선, 비교 모드, 로딩·실패·빈 상태를 보조기술에 전달한다.
- 색상만으로 노선·오류 유형·데이터 상태를 구분하지 않고 텍스트·아이콘·범례를 함께 사용한다.
- 본문 텍스트와 disabled 상태의 대비를 유지한다.
- 하단 고정 내비게이션과 모달은 `env(safe-area-inset-bottom)` 및 `dvh`를 사용한다.
- 320px 수준의 좁은 폭, 가로 방향, 낮은 viewport 높이에서도 지도·시간표·비교함에 가로 overflow가 생기지 않게 한다.

## 10. 구현 매핑과 검수 기준

### 실제 소스 매핑

| 영역 | 현재 소스 | 적용 내용 |
| --- | --- | --- |
| 디자인 토큰·공통 CSS | `app/globals.css` | 색상·글꼴·카드·입력·버튼·포커스·반응형 토큰 반영 |
| 앱 셸·내비게이션·푸터 | `app/layout.tsx` | 사이드바/모바일 내비게이션, 브랜드, 정책·문의 링크 스타일 반영 |
| 홈 콘텐츠 | `app/page.tsx` | eyebrow·제목·상태 고지·도움말 카드 스타일 반영 |
| 검색·비교·지도·시간표 | `components/ShuttleExplorer.tsx` | 셔틀 기능을 유지한 채 카드·색상·반응형·상태 스타일 반영 |
| 카카오 지도 | `components/KakaoMapWrapper.tsx`, `components/MapPreview.tsx` | 지도 표면·빈 상태·오류 상태 토큰 반영 |
| 광고 배너·동의 | `components/CoupangBanner.tsx`, `components/PrivacyConsent.tsx` | 옐로 soft 표면, 모달·접근성 상태 반영 |
| 운영·개인정보·약관·문의 | `app/operations/page.tsx`, `app/privacy/page.tsx`, `app/terms/page.tsx`, `app/contact/page.tsx` | 공통 카드·세리프 제목·행간·경고 상태 반영 |
| 관리자 | `app/editor/page.tsx` | 동일 토큰을 사용하되 오류 검토 영역 대비 강화 |

### 검수 체크리스트

- 기존 센터·근무조·노선 선택과 정류장 표시가 동일하게 동작한다.
- 센터 파일 지연 로딩과 전체 JSON 폴백이 동일하게 동작한다.
- 비교함 최대 5개, 1시간 잠금 해제, 앱 메시지, 웹 제휴 링크 흐름이 깨지지 않는다.
- 카카오 지도 핀, 노선 색상, 시간표 정렬, 모바일 카드 전환을 확인한다.
- 광고 쿠키 동의 전후와 쿠키 설정 버튼을 확인한다.
- `/operations`, `/privacy`, `/terms`, `/contact`, `/editor`의 링크와 접근 제한을 확인한다.
- 1280/1024/820px 및 560/420/390px에서 줄바꿈·정렬·overflow를 확인한다.
- `prefers-reduced-motion`, 키보드 탐색, 포커스, `aria-live` 상태를 확인한다.
- 변경 후 `git diff --check`와 프로덕션 빌드를 실행한다.

## 11. 적용 가능성

이 문서는 현재 Next.js 앱에 적용할 수 있습니다. `design.md`의 Rails 경로와 ERB 뷰를 직접 가져오지 않고, 실제 소스 매핑 표의 TSX·CSS 파일을 수정하는 방식으로 적용합니다.

기능 구현에 필요한 카드, 색상, 반응형, 모달, 지도 표면은 현재 Tailwind와 일반 CSS로 구현할 수 있습니다. 별도 디자인 라이브러리나 데이터베이스 변경은 필요하지 않습니다. 브랜드 세리프 폰트 파일만 현재 저장소에 없으므로 `Noto Serif KR`·`Georgia` 폴백을 사용하거나 나중에 폰트 파일을 추가하면 됩니다.

적용 순서는 `app/globals.css`의 토큰과 공통 컴포넌트 → `app/layout.tsx` 셸 → `app/page.tsx` 홈 → `ShuttleExplorer` 및 지도·모달 → 정책·문의·관리자 화면 → 반응형·접근성 검수로 정합니다.

이 문서는 디자인 기준이며, 동작 로직·데이터 병합·배포 설정을 대신하지 않습니다.
