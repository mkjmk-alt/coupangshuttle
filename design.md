# 연차, 영차! 웹 디자인 시스템

> 이 문서는 현재 웹 구현의 디자인 기준을 한곳에 모은 문서입니다. 실제 적용값은 `app/assets/stylesheets/application.css`와 각 화면의 ERB 뷰를 기준으로 하며, 아래 치수는 미디어 쿼리에서 조정될 수 있는 기본값입니다.

## 1. 제품 인상과 목표

`연차, 영차!`는 받은 휴가와 사용한 휴가를 가볍게 기록하고 남은 휴식을 한눈에 확인하는 서비스입니다.

- 휴가 기록을 가장 먼저 발견할 수 있게 한다.
- 업무 도구처럼 딱딱하기보다 휴식과 어울리는 차분한 분위기를 만든다.
- 산뜻한 코랄 색상으로 행동을 유도하고, 민트·블루로 보조 상태를 구분한다.
- 큰 세리프 제목과 읽기 쉬운 산세리프 본문을 대비시켜 브랜드성을 유지한다.
- 데스크톱, 태블릿, 모바일에서 같은 정보 구조와 테마를 유지한다.

## 2. 디자인 원칙

1. **기록 우선** — 홈에서 잔여 휴가, 사용 현황, 최근 기록, 새 기록 버튼이 자연스럽게 이어진다.
2. **명확한 역할 분리** — 코랄은 주요 행동·사용 휴가, 민트는 받은 휴가·휴무, 블루는 안내·선택 상태에 사용한다.
3. **세리프와 산세리프의 역할 분담** — 제목·날짜·숫자는 표현력을, 본문·컨트롤은 가독성을 우선한다.
4. **카드 안의 질서** — 넉넉한 내부 여백, 얇은 선, 낮은 대비의 그림자로 정보 덩어리를 구분한다.
5. **점진적 반응성** — 화면 폭이 줄어들면 열 수를 줄이고, 컨트롤을 재배치하며, 기능은 유지한다.

## 3. 시각 토큰

### 색상

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `--paper` | `#f7f6f2` | 전체 페이지 배경 |
| `--card` | `#fffefb` | 카드·모달 표면 |
| `--ink` | `#242422` | 본문·제목 |
| `--muted` | `#77766f` | 보조 설명·비활성 텍스트 |
| `--line` | `#e7e4dc` | 카드·입력 필드 테두리 |
| `--soft-line` | `#efede7` | 구분선·약한 경계 |
| `--coral` | `#e96f50` | 주요 버튼, 사용 휴가, 강조 |
| `--coral-dark` | `#cc593d` | 코랄 버튼 hover/pressed |
| `--coral-soft` | `#fff0e9` | 코랄 배경·선택 상태 |
| `--mint` | `#65ad94` | 받은 휴가, 고정·변동 휴무 |
| `--mint-soft` | `#eaf6f1` | 민트 배경·성공 상태 |
| `--blue` | `#7d9ce4` | 정보, 토요일, 선택 보조 상태 |
| `--blue-soft` | `#edf2ff` | 안내 패널 배경 |
| `--violet` | `#9a82cb` | 보조 카테고리 |
| `--violet-soft` | `#f2edfb` | 보조 카테고리 배경 |
| `--yellow` | `#d3a73e` | 주의·대기 상태 |
| `--yellow-soft` | `#fff7d9` | 주의 상태 배경 |

페이지 배경은 `--paper` 위에 우측 상단의 옅은 복숭아색 radial gradient를 겹친다. 강한 색상은 CTA와 상태에만 사용하고, 표면은 거의 흰색으로 유지한다.

### 글꼴

| 역할 | 글꼴 | 기본 크기·굵기 | 적용 예 |
| --- | --- | --- | --- |
| 본문·버튼·라벨 | `Pretendard`, `Apple SD Gothic Neo`, `Noto Sans KR`, 시스템 sans-serif | 10–16px, 400–750 | 설명, 폼, 내비게이션 |
| 로고·브랜드명 | `Yeoncha Brand Serif`, `Noto Serif KR`, serif | 17–18px, 600 | 로고의 “연차, 영차!” |
| 큰 제목·월·날짜·숫자 | `Georgia`, `Noto Serif KR`, serif | 제목 24–58px, 숫자 64px까지 | 화면 제목, 잔여 일수, 월 라벨 |
| 초대 코드 | `ui-monospace`, `SFMono-Regular`, `Menlo`, monospace | 컨텍스트에 맞춤 | 일회용 초대 코드 |
| 섹션 eyebrow | 본문 글꼴 | 11–12px, 800, 자간 약 `.06em` | `READY, SET, REST!`, `USED BY TYPE` |

웹에서 제공하는 유일한 번들 폰트는 `app/assets/fonts/noto-serif-kr-semibold-brand.woff2`이며 `Yeoncha Brand Serif`로 등록한다. 세리프 글꼴의 한글 fallback은 기기별로 다를 수 있으므로, 본문용 글꼴을 제목에 섞어 쓰지 않는다.

### 형태·그림자·모션

- 기본 카드: 반지름 `22px`, `1px solid var(--line)`, `var(--shadow)`.
- 작은 카드·입력·버튼: 반지름 `11–16px`.
- pill/badge: 반지름 `99px`.
- 기본 그림자: `0 18px 50px rgba(43, 40, 32, .055)`처럼 매우 낮은 대비를 사용한다.
- 버튼 hover는 코랄을 어둡게 하고 `translateY(-1px)`만 적용한다.
- 화면 진입은 opacity와 `translateY(6px)`를 약 420ms 동안 사용한다.
- `prefers-reduced-motion: reduce`에서는 전환·애니메이션을 최소화한다.

## 4. 레이아웃

### 앱 셸

- 전체 셸은 `.app-shell`이며 최소 높이는 `100vh`다.
- 데스크톱 사이드바는 폭 `248px`, 뷰포트에 고정하고 반투명 흰색·blur·우측 경계를 사용한다.
- 본문 `.page-shell`은 사이드바 폭만큼 왼쪽 여백을 둔다.
- 상단바 높이는 약 `88px`, 좌우 패딩은 `clamp(26px, 5vw, 72px)`다.
- 메인 콘텐츠는 최대 폭 `1280px`, 상단 `56px`, 하단 `100px` 패딩으로 중앙 정렬한다.
- 모바일에서는 사이드바가 하단 고정 내비게이션으로 바뀌며, safe-area inset을 고려한다.

### 여백 스케일

컴포넌트 간격은 `8, 10, 12, 14, 16, 18, 20, 24, 28, 32, 36, 44, 56, 72px`를 기본으로 한다. 작은 요소 안에서는 8–14px, 카드 내부에서는 20–32px, 주요 섹션 사이에서는 24–56px를 우선한다.

### 반응형 기준

| 기준 폭 | 동작 |
| --- | --- |
| `1120px` 이하 | 홈·그룹 그리드 축소, 사용 유형 카드 열 수 감소 |
| `980px` 이하 | 기록 표를 카드형 행으로 전환, 주요 그리드 축소 |
| `820px` 이하 | 사이드바를 모바일 내비게이션으로 전환, 모달·폼을 세로화 |
| `620px` 이하 | 달력 컨트롤과 요약 pill을 줄이고 필요하면 줄바꿈 |
| `560px` 이하 | 메인/상단바 패딩 14px 수준, 제목·카드·폼 축소 |
| `420px` 이하 | 게스트 액션을 2열 + 주요 버튼 전체 폭으로 배치 |
| `390px` 이하 | 월·날짜·이벤트 라벨을 추가 축소 |

높이가 낮은 데스크톱에서는 사이드바 자체가 넘치지 않도록 overflow를 보정한다. 모바일 화면은 `100dvh`, 상단·하단 `env(safe-area-inset-*)`를 사용한다.

## 5. 공통 컴포넌트

### 버튼과 링크

- **Primary**: 코랄 배경, 흰색 텍스트, 최소 높이 `48px`, 좌우 패딩 `20px`, 반지름 `13px`, 굵기 `750`.
- **Compact primary**: 최소 높이 `40px`, 반지름 `11px`, 카드 헤더나 작은 CTA에 사용한다.
- **Secondary**: 카드 배경, 얇은 테두리, 높이 약 `41px`, 반지름 `11px`, muted 텍스트.
- **Text link**: 배경과 테두리 없이 사용하며, 화살표나 강조 부분만 코랄로 표시한다.
- 모든 버튼과 입력은 `font: inherit`로 본문 글꼴을 공유한다.

### 카드·배지

카드는 `var(--card)` 표면과 얇은 `var(--line)` 테두리를 사용한다. 카드 안에서 제목 → 설명 → 수치/행동의 순서를 지키고, 그림자는 경계를 보조하는 정도로만 사용한다. 상태 배지는 짧은 텍스트와 pill 형태를 사용하며 코랄·민트·블루·옐로 soft 토큰과 짝을 이룬다.

### 입력·선택 컨트롤

입력·select·날짜 트리거는 높이 `44–48px`, 반지름 `11px`, `#faf9f6` 또는 `#fbfaf7` 표면을 기본으로 한다. 포커스 시 코랄 계열 `3px` outline과 `2px` offset을 제공한다. 오류 메시지는 해당 입력 그룹 가까이에 배치해 화면 하단으로 밀리지 않게 한다.

### 달력

- 요일 헤더는 낮은 대비의 `#f5f3ed` 배경, 7열 grid를 사용한다.
- 날짜 셀은 기본 흰색 표면과 얇은 선, 모바일에서 최소 높이 약 `88px`(화면별 조정)을 사용한다.
- 오늘은 코랄 원형 표시, 선택일은 코랄, 범위 안 날짜는 옅은 코랄, 휴무일은 민트, 공휴일은 코랄 계열 soft로 표시한다.
- 월 이동 컨트롤은 월 라벨 양옆에 배치하고, 작은 폭에서도 월 라벨이 중앙에 남도록 `minmax(0, 1fr)`을 사용한다.
- 달력 팝오버는 폭 `min(286px, calc(100vw - 42px))`, 반지름 `16px`, 그림자와 테두리를 사용한다.
- 모달 안의 달력은 현재 서비스 카드와 동일한 종이색·코랄·민트 테마를 공유한다.

### 모달·확인창

- 배경은 어두운 반투명 overlay와 blur를 사용한다.
- 일반 달력/휴가 모달은 폭 `min(520px, calc(100vw - 28px))`, 반지름 `22px`, viewport-safe max-height를 사용한다.
- 헤더는 `linear-gradient(145deg, #fffdf9, var(--coral-soft))`, 제목은 Georgia/Noto Serif KR 약 `27px`다.
- 삭제 확인창은 폭 `min(420px, calc(100vw - 32px))`, 반지름 `24px`이며, 위험 행동을 코랄로 명확히 구분한다.
- 모달이 열린 동안 모바일 하단 내비게이션은 숨기고, 본문 스크롤과 키보드 영역을 분리한다.

### 기록 목록

데스크톱에서는 날짜·종류·일수·메모·행동을 표 형태로 정렬한다. `980px` 이하에서는 각 기록을 독립 카드로 바꾸고, 종류/행동/일수/날짜/메모가 일정한 grid 영역을 사용한다. 기록 사이에는 충분한 수직 여백과 선 또는 카드 경계를 두어 인접 항목이 하나로 보이지 않게 한다.

## 6. 화면별 구성

### 홈

1. `READY, SET, REST! · 연도` eyebrow와 사용자 인사.
2. 잔여 연차 balance 카드: 누적 기준, 큰 잔여 숫자, 받은/사용한 메타, donut 비율, 전체 기록 링크.
3. 받은 연차·사용한 연차 stat strip.
4. `USED BY TYPE` 유형별 사용 휴가 카드 grid.
5. `RECENT RECORDS` 최근 기록 activity list.
6. `HOW IT WORKS` 3단계 안내와 새 기록 CTA.

잔여·받은 값은 민트 계열, 사용 값과 주요 행동은 코랄 계열로 연결한다. 설명 문장은 수치의 의미를 보조하되 제목과 겹치거나 불필요하게 길어지지 않도록 한다.

### 내 캘린더

`MY CALENDAR` eyebrow, “나의 쉼을 한눈에 봐요” 제목, 설명, 휴무일 설정/휴가 기록 actions 뒤에 개인 달력 카드를 둔다. 달력에는 월 이동, 요약 pill, 범례, 날짜 grid, 이벤트 칩, 빈 상태를 포함한다.

### 휴가 기록·전체 기록

`LEAVE RECORDS` 또는 `ALL RECORDS` 제목 아래 연도 선택과 CSV 내려받기를 제공한다. 받은 휴가/사용한 휴가를 색상과 부호로 구분하고, 기록 카드는 수정·삭제 행동을 항상 같은 위치에 둔다. 연도별 보기와 누적 보기는 탭/선택 상태로 명확히 구분한다.

### 모임 캘린더

그룹 이름과 설명, 멤버 수, 내 캘린더 공유 범위, 일회용 초대 코드, 멤버 목록 순서로 구성한다. 공유 범위는 동일한 라디오 옵션 카드·select 스타일을 사용하고, 공유 중/중지 상태를 badge로 표시한다. 그룹 달력은 개인 달력과 같은 grid·범례·월 이동 규칙을 사용한다.

### 설정·계정·법적 페이지

설정 카드는 공통 카드 토큰을 사용하며, 섹션 제목은 Georgia 약 `24–31px`, 폼 컨트롤은 높이 `48px`다. 토글 행은 최소 높이 `68px`, 둥근 `#faf9f6` 표면으로 제공한다. 계정/로그인 화면은 데스크톱에서 폼과 장식 영역의 2열, 모바일에서 장식 영역을 숨긴 단일 열로 전환한다. 법적 문서는 같은 종이·카드·세리프 제목과 넉넉한 행간(약 `1.9`)을 사용한다.

### 게스트 모드

게스트 화면은 브랜드 topbar, `GUEST MODE` 안내, 기기 저장 상태 pill, 휴무일 설정/연차 지급 설정/휴가 기록 actions, 개인 달력, 로컬 기록, 계정 병합 안내 순서다. 로그인 없이도 같은 달력·모달·버튼 테마를 유지하고, 모바일에서는 actions를 2열 + 주요 행동 전체 폭으로 재배치한다.

## 7. 상태와 상호작용 규칙

- hover/pressed/selected/disabled 상태는 색상뿐 아니라 테두리·outline·명도 변화로도 구분한다.
- 키보드 포커스는 모든 버튼·링크·입력에서 보이는 코랄 outline을 제공한다.
- 삭제처럼 되돌리기 어려운 행동은 현재 테마의 확인 모달을 거친다. 모달을 닫는 X는 저장/삭제를 실행하지 않는다.
- 저장·병합·삭제 후에는 결과를 `aria-live` 영역과 시각적 toast/상태 문구로 알린다.
- 날짜 선택은 시작일·종료일을 같은 달력 컨텍스트에서 확인할 수 있게 하며, 이미 등록된 휴가·휴무일은 선택 불가 상태를 명시한다.
- 게스트 기록은 기기에 저장하고, 로그인 후 계정 병합 시 로컬 기록과 서버 기록의 결과를 사용자에게 설명한다.
- 로딩·실패·빈 상태에도 레이아웃 점프가 생기지 않도록 최소 영역을 유지한다.

## 8. 접근성과 모바일 안전 영역

- 제목 계층과 landmark를 화면 구조와 일치시킨다.
- 아이콘만 있는 버튼에는 `aria-label`을 제공하고, 선택된 탭·요일·날짜에는 선택 상태를 전달한다.
- 색상만으로 받은/사용한 휴가를 구분하지 않고 부호·텍스트·범례를 함께 사용한다.
- 본문 최소 대비를 유지하고, disabled 상태도 읽을 수 있는 대비를 확보한다.
- 하단 고정 내비게이션과 모달은 `env(safe-area-inset-bottom)` 및 `dvh`를 사용해 iOS의 홈 인디케이터와 키보드에 가리지 않게 한다.
- 320px 수준의 좁은 폭, 가로 방향, 낮은 viewport 높이에서도 가로 overflow가 생기지 않는지 확인한다.

## 9. 구현·검수 체크리스트

- 새 컴포넌트는 먼저 색상·간격·글꼴 토큰을 재사용한다.
- 임의의 색상값, 별도 그림자, 임의의 제목 글꼴을 추가하기 전에 기존 토큰으로 해결할 수 있는지 확인한다.
- 데스크톱 폭(1280/1024/820px)과 모바일 폭(560/420/390px)에서 줄바꿈·정렬·overflow를 확인한다.
- 달력 월 이동, 날짜 범위 선택, 팝오버, 모달 닫기, 삭제 확인, 입력 오류를 실제 흐름으로 점검한다.
- 기록 카드의 위아래 경계와 숫자 색상이 유형별로 일관적인지 확인한다.
- `prefers-reduced-motion`, 키보드 탐색, screen reader용 label/live region을 점검한다.
- 변경 후 `git diff --check`를 실행하고, 관련 뷰의 스크린샷으로 시각적 회귀를 확인한다.

## 10. 소스 맵

| 영역 | 주요 파일 |
| --- | --- |
| 디자인 토큰·공통 CSS | `app/assets/stylesheets/application.css` |
| 홈 | `app/views/dashboard/_home.html.erb` |
| 내 캘린더 | `app/views/dashboard/_calendar.html.erb` |
| 전체 기록 | `app/views/dashboard/_records.html.erb` |
| 모임 캘린더 | `app/views/dashboard/_groups.html.erb` 및 그룹 캘린더 뷰 |
| 게스트 | `app/views/guest/show.html.erb` |
| 인증·계정·설정 | `app/views/...`의 auth/account/settings 뷰 |
| 브랜드 폰트 | `app/assets/fonts/noto-serif-kr-semibold-brand.woff2` |
| 로고·앱 아이콘 | `public/app-icon*.png/svg`, `public/favicon.png`, `public/apple-touch-icon.png`, `public/og.png` |

이 문서는 디자인 의사결정과 검수 기준을 설명하는 문서이며, 동작 로직이나 배포 설정을 대신하지 않는다. 구현 변경 시 관련 CSS와 화면 소스를 함께 갱신한다.
